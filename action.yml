name: 'Slack Deploy Pipeline Notifications'
description: 'Post deploy workflow progress notifications to Slack'
author: 'Fieldguide'
inputs:
  thread_ts:
    description: 'Initial Slack message timestamp ID'
    required: false
    # Distinguish an omitted input from one explicitly set to an empty string;
    # see SUMMARY_THREAD_TS in src/utils/postMessage.ts
    default: 'summary'
  conclusion:
    description: "'true' denotes last stage"
    required: false
  github_token:
    description: 'Repository GITHUB_TOKEN or personal access token secret'
    required: false
    default: ${{ github.token }}
  status:
    description: 'The current status of the job'
    required: false
    default: ${{ job.status }}
outputs:
  ts:
    description: 'Slack message timestamp ID'
runs:
  using: 'node24'
  main: 'dist/notifySlack/index.js'
branding:
  icon: bell
  color: purple
